#!/usr/bin/env python3
import sys

if __name__ == "__main__":
    # Boring mapper, because we have all the information (page name, date and page views), there is no need to change
    for line in sys.stdin:
        print(line)